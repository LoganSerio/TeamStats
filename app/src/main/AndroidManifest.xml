<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.logan.test2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomepageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".SendFeedbackActivity" />
        <activity android:name=".AddPositionActivity" />
        <activity android:name=".ListPositionsActivity" />
        <activity android:name=".ListTeamsActivity" />
        <activity
            android:name=".CreateTeamActivity"
            android:theme="@style/AppTheme.myTheme" />
        <activity android:name=".PositionStats" />
        <activity
            android:name=".TeamPage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddStatisticsActivity" />
        <activity android:name=".ListStatisticsActivity" />
    </application>

</manifest>