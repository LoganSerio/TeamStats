<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_settings"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.logan.test2.Settings">

    <Button
        android:text="@string/sendFeedbackButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="99dp"
        android:id="@+id/sendFeedbackButton" />

    <Button
        android:id="@+id/reorderTeamsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/sendFeedbackButton"
        android:layout_alignStart="@+id/sendFeedbackButton"
        android:layout_below="@+id/sendFeedbackButton"
        android:layout_marginTop="30dp"
        android:text="User Manual"
        android:layout_alignRight="@+id/sendFeedbackButton"
        android:layout_alignEnd="@+id/sendFeedbackButton" />

    <Button
        android:id="@+id/btnTempTeamPage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/reorderTeamsButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="68dp"
        android:text="Temp Team Page" />
</RelativeLayout>
